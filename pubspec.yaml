name: yingyin_shijie
description: 影音视界 Flutter 跨平台壳应用，支持 Android 与 iOS。
publish_to: "none"
version: 3.6.9+369

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
